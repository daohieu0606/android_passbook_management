<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.passbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp">
        <activity android:name=".activities.TestLibActivity">
        </activity>
        <activity android:name=".activities.PickUpChangeRegulationTypeActivity" />
        <activity android:name=".activities.SelectMonthToGetReportActivity" />
        <activity android:name=".activities.PickupReportActivity" />
        <activity android:name=".activities.MonthlyReportActivity" />
        <activity android:name=".activities.DateReportActivity" />
        <activity android:name=".activities.RegisterPassBookActivity" />
        <activity android:name=".activities.EditDepositActivity" />
        <activity android:name=".activities.EditWithdrawalActivity" />
        <activity android:name=".activities.TestDataActivity" >

        </activity>
        <activity android:name=".activities.BaseActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>